import React from 'react';
import {
    View,
    Button,
} from 'react-native';
import { TextInput } from 'react-native-paper';

const LoginScreen = () => {
    return (
        <TextInput
        label="Email"
        value="aaaaaaaaaaaa"
        />
    );
};


export default LoginScreen;
